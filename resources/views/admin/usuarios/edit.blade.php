@extends('layouts.app')
@section('title', 'Editar usuario | CPIMuni')
@section('page-title', 'Editar usuario')
@section('content')
<section class="card edit-card"><div class="card-title">Actualizar cuenta</div>@include('partials.form-feedback')<form method="POST" action="{{ route('admin.usuarios.update', $usuario) }}" class="form-grid">@csrf @method('PUT')<label>Nombre<input name="nombre" required value="{{ old('nombre', $usuario->nombre) }}"></label><label>Apellido<input name="apellido" value="{{ old('apellido', $usuario->apellido) }}"></label><label>Correo electrónico<input type="email" name="email" required value="{{ old('email', $usuario->email) }}"></label><label>Nueva contraseña<input type="password" name="password" minlength="8" placeholder="Dejar vacía para conservarla"></label><label>Rol<select name="rol_id" required>@foreach($roles as $rol)<option value="{{ $rol->id }}" @selected($usuario->rol_id === $rol->id)>{{ $rol->nombre }}</option>@endforeach</select></label>@if(auth()->user()->rol?->nombre === 'SUPERADMIN')<label>Municipalidad<select name="municipalidad_id"><option value="">Cuenta global</option>@foreach($municipalidades as $municipalidad)<option value="{{ $municipalidad->id }}" @selected($usuario->municipalidad_id === $municipalidad->id)>{{ $municipalidad->nombre }}</option>@endforeach</select></label>@endif<label>Estado<select name="estado"><option @selected($usuario->estado === 'ACTIVO')>ACTIVO</option><option @selected($usuario->estado === 'INACTIVO')>INACTIVO</option><option @selected($usuario->estado === 'BLOQUEADO')>BLOQUEADO</option></select></label><div class="actions"><a class="button secondary" href="{{ route('admin.usuarios') }}">Cancelar</a><button class="button" type="submit">Guardar cambios</button></div></form></section>
@endsection
@push('styles')<style>.edit-card{max-width:760px}.form-grid{display:grid;grid-template-columns:1fr 1fr;gap:14px}label{font-size:11px;font-weight:700;color:#475569}input,select{display:block;width:100%;margin-top:6px;padding:10px 11px;border:1px solid #cbd5e1;border-radius:8px;background:#fff;font:inherit;font-size:13px}.actions{grid-column:span 2;display:flex;gap:8px;margin-top:10px}.button{border:0;border-radius:8px;padding:11px 15px;background:#2563eb;color:#fff;font-weight:700;text-decoration:none;cursor:pointer}.secondary{background:#e2e8f0;color:#334155}.alert{padding:10px 12px;border-radius:8px;margin-bottom:14px;font-size:12px}.alert.error{background:#fee2e2;color:#991b1b}@media(max-width:700px){.form-grid{grid-template-columns:1fr}.actions{grid-column:auto}}
</style>@endpush
